---
apiVersion: v1
kind: Service
metadata:
  name: subscription-service
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    targetPort: 8080
    port: 8080
  selector:
    app: subscription-service